{% extends 'base.html.twig' %}

{% block body %}
    <div class="mt-6 uppercase font-bold">order info</div>
    <div class="mt-6">
        <div class="grid grid-cols-3 justify-center py-2 gap-4">
            <hr class="col-span-3">
            <div class="font-bold">ID</div>
            <div class="font-bold">Name</div>
            <div class="font-bold">Price</div>
            <hr class="col-span-3">
            {% for orderDetail in orderDetails %}
                <div>
                    {{ orderDetail.id }}
                </div>
                <div>
                    {{ orderDetail.name }}
                </div>
                <div>
                    {{ orderDetail.price }}
                </div>
                <hr class="col-span-3">
            {% endfor %}
            <div class="col-start-3">
                <div class="font-bold uppercase">total</div>
                <div>{{ orderPaid.total }}</div>
            </div>
        </div>
    </div>
    <div class="mt-6">
        <div class="font-bold uppercase">you won a new voucher to use on your next purchase!</div>
        <div class="flex justify-center mt-2">
            <div class="rounded p-4 bg-blue-200 uppercase text-sm">{{ newVoucherCode }}</div>
        </div>
    </div>
{% endblock %}